export const selectCities = (state) => state.cities.cities;
export const selectWeather = (state) => state.cities.cityWeather;
export const selectCurrentWeather = (state) => state.cities.currentWeather;
